# 2026-02-09

## #1 Query Handling
我在Node中添加了is_query属性，用于标记这个节点是否是一个query节点（例如疑问词）。
此外我加入了query_type属性，用于标记具体是哪种query（例如WHOSE, WHAT, WHICH等）。
你需要基于实际情况，在denial comment中处理这些query节点。

## #2 ADV
我吧ADV也纳入了vertex的候选范围，请注意下这样可能会带来的不良问题。

## #3 semantic cluster（HS）

Query 上搜索，在Data上面通过映射来找到对应的实体。
(u, v) Arching
HS0, HS0' hop = 0
HS1, HS1' <- e': v -> v', e' cap pre_e
-> ((HS, HS')_new + (HS, HS')_pre)_k

HS, HS'
0. HS + e
1. u' in HS
2. v' <- DC(u') |_m 
3. (u -> u': HS; v -> v': HS') similarity
