# 2026-03-12

## #1 Run the alg
首先确定算法的时间效率，看看相比之前是否有显著的提升。

其次，测试算法的效果。

算法的几个优化方向：
- Query 节点的选择：
    - 我们可以只枚举 VERB, Ent 的节点
    - 目前 Query 侧 基本上只有一个 VERB, 可以采用某种方式，先匹配Data侧的一个VERB，然后围绕着它，匹配对应的其他的节点。
    - AUX 的情况枚举剩余的两个节点即可
- 一部分的节点应该采取Ent匹配 + Cosine 的方式
- Data SC 的构建。
    - 可以用一些辅助数据结构，先筛掉哪些不可达的 (v, v') 基于连通性
    - 我们可能需要合并复数的 path 成为一个 SC
- threshold 的选择
- SC 对应的Text的构建也是个问题


## #2 RAG
在本周之内，完成RAG上的基本的优化，开始跑大实验。


